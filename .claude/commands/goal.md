---
description: Create a new goal linked to your values
argument-hint: <what you want to achieve>
---

Create a goal note in `02-Goals/` from `_templates/Goal.md` for:
**$ARGUMENTS**

Before writing it, ask the user (in one message):
1. Which of their [[Values]] it serves — offer your best guess.
2. Target date.
3. How they'll measure "achieved".

Then fill the template, propose 3–5 milestones for them to react to, link
the goal into `02-Goals/Goals.md`, and rebuild the Command Center. If
`$ARGUMENTS` is empty, ask what the goal is first.
