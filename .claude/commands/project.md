---
description: Spin up a new project with outcome and next actions
argument-hint: <the project>
---

Create a project note in `03-Projects/` from `_templates/Project.md` for:
**$ARGUMENTS**

Draft the one-sentence Outcome and the first 3 next actions yourself from
what the user said — show them for correction rather than interrogating
them. Ask only what you can't infer (due date? linked to a goal?).

Link it into `03-Projects/Projects.md` with a 🟢 status and rebuild the
Command Center. If `$ARGUMENTS` is empty, ask what the project is.
