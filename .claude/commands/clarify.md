---
description: Process every Inbox item into the right section (GTD-style)
---

Read `00-Inbox/Inbox.md` and process every capture using the **Clarify
logic** in CLAUDE.md. Create notes from `_templates/` where needed, add
tasks to existing notes where they belong, and link new notes into their
section index.

If an item is genuinely ambiguous, ask the user about it — batch your
questions rather than asking one at a time.

When done: leave the Inbox empty (just the heading), update every touched
index, rebuild `Command Center.md`, and report what went where as a short
bullet list.
